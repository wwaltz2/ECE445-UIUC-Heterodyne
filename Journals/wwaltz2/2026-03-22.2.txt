Made some updates to the schematic in kicad. We will have to have two pin headers, ideally one which can be plugged into the USBasp to program the MCU, and one that can be connected via bare wires to the Arduino Uno (maybe screw terminal blocks) to program the flash memory. 

We will also need to step down the voltage (via voltage divider) of the Arduino Uno outputs as to not fry our Flash Mem.

The PCB should just be labeled by Flash Mem pin numbers, to be honest.

Chip select and audio EN nodes are effectively the same node. I will test to see if we need that connection between MCU and Flash tomorrow.

Finally, we need to change the footprint of the switches for reset and chip select to be actual switches and not buttons.